# 高质量医疗指标管理系统使用说明

## 目录
1. [系统概述](#系统概述)
2. [指标项管理](#指标项管理)
3. [指标管理](#指标管理)
4. [指标计算](#指标计算)
5. [结果查看](#结果查看)
6. [科室下钻功能](#科室下钻功能)

---

## 系统概述

高质量医疗指标管理系统是一个用于管理和计算医疗质量指标的综合平台。系统采用树形结构组织指标,支持复杂的指标计算和科室维度的数据下钻分析。

### 核心功能
- **指标项管理**: 配置基础数据查询SQL
- **指标管理**: 构建指标树形结构,配置计算表达式
- **指标计算**: 批量执行指标计算
- **结果查看**: 多维度查看计算结果
- **科室下钻**: 按科室维度分析指标数据

---

## 指标项管理

指标项是系统的基础数据单元,每个指标项对应一个SQL查询。

### 创建指标项

#### 步骤1: 进入指标项管理页面
- 点击左侧菜单"指标项管理"

#### 步骤2: 点击新增按钮
- 点击页面右上角"新增指标项"按钮

#### 步骤3: 填写基本信息

| 字段 | 说明 | 示例 | 必填 |
|------|------|------|------|
| 指标项名称 | 指标项的中文名称 | 成人患者数 | ✅ |
| 指标项编码 | 字母+数字格式,唯一标识 | A0001, a0041 | ✅ |
| 指标项类型 | 采集或计算 | 采集 | ✅ |
| 数据源 | 数据表名称 | D_MR | ✅ |
| 单位 | 计量单位 | 人、例、% | ❌ |

**编码格式要求**: 必须以字母开头,后跟数字
- ✅ 正确: `A0001`, `a0041`, `B1234`
- ❌ 错误: `0001A`, `A-001`, `123`

#### 步骤4: 配置查询SQL

**重要说明**:

**1. 时间过滤条件** (必须)
SQL结尾必须加上时间过滤条件,确保时间选择逻辑贯通:
```sql
STR_TO_DATE(字段名, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
```

**2. 科室下钻别名** (支持下钻时必须)
如果指标需要支持科室下钻,聚合函数必须添加别名 `AS result_value`:
```sql
SELECT COUNT(*) AS result_value FROM ...
```

⚠️ **若缺少别名,科室下钻时程序将无法识别返回值,导致无下钻结果**

**完整示例**:
```sql
SELECT COUNT(DISTINCT A48, A49) AS result_value
FROM d_mr
WHERE A14 >= 18
AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
```

#### 步骤5: 保存
- 点击"确定"按钮保存指标项

### 修改指标项

#### 步骤1: 查找指标项
- 在搜索框输入指标项名称或编码进行搜索

#### 步骤2: 点击编辑
- 在目标指标项行点击"编辑"按钮

#### 步骤3: 修改信息
- 修改需要调整的字段
- **注意**: 指标项编码一旦创建通常不建议修改

#### 步骤4: 保存
- 点击"确定"按钮保存修改

### 测试指标项

在保存指标项后,建议先测试SQL是否正确:

#### 步骤1: 点击测试按钮
- 在指标项行点击"测试"按钮

#### 步骤2: 选择测试年份
- 选择一个有数据的年份(如2020、2023)
- 系统自动设置时间范围为该年度的1月1日至12月31日

#### 步骤3: 执行测试
- 点击"执行"按钮
- 查看返回的结果值

---

## 指标管理

指标采用树形结构组织,分为非叶子节点(分组)和叶子节点(可计算的指标)。

### 创建非叶子节点(分组)

非叶子节点用于组织和分类指标,不参与实际计算。

#### 步骤1: 进入指标管理页面
- 点击左侧菜单"指标管理"

#### 步骤2: 点击新增按钮
- 点击左侧指标树卡片的"新增"按钮

#### 步骤3: 填写基本信息

| 字段 | 说明 | 示例 | 必填 |
|------|------|------|------|
| 指标名称 | 指标的中文名称 | 医疗质量指标 | ✅ |
| 指标编码 | 自动跟随名称生成 | 医疗质量指标 | ✅ |
| 父节点 | 选择上级节点,不选则为根节点 | - | ❌ |
| 层级 | 自动根据父节点层级+1计算 | 1 | ✅ |
| 是否末级 | 选择"否" | 否 | ✅ |
| 指标类型 | 选择类型 | 质量指标 | ✅ |
| 显示顺序 | 在同级节点中的排序 | 1 | ✅ |

**重要提示**:
- 指标编码默认跟随名称自动生成
- 选择父节点后,层级会自动计算为父节点层级+1
- 非叶子节点不需要配置计算相关字段

#### 步骤4: 保存
- 点击"确定"按钮保存

### 创建叶子节点(可计算指标)

叶子节点是实际可以计算的指标。

#### 步骤1: 点击新增按钮
- 在指标树中选择要添加子节点的父节点
- 点击"新增"按钮

#### 步骤2: 填写基本信息

| 字段 | 说明 | 示例 | 必填 |
|------|------|------|------|
| 指标名称 | 指标的中文名称 | 剖宫产率 | ✅ |
| 指标编码 | 自动跟随名称生成 | 剖宫产率 | ✅ |
| 父节点 | 选择上级分组节点 | 医疗质量指标 | ✅ |
| 层级 | 自动计算 | 2 | ✅ |
| 是否末级 | **选择"是"** | 是 | ✅ |
| 指标类型 | 选择类型 | 质量指标 | ✅ |

#### 步骤3: 配置计算方式

**选择计算类型**:

##### 方式1: 单指标项 (ITEM)
直接使用一个指标项的结果,不需要表达式

- **计算类型**: 选择"单指标项"
- **关联指标项**: 选择一个指标项
- **表达式**: 留空
- **单位**: 继承指标项单位或自定义

**示例**: 总病案数
- 计算类型: 单指标项
- 关联指标项: `a0001` (总病案数指标项)
- 表达式: (留空)

##### 方式2: 表达式计算 (EXPRESSION)
通过数学表达式组合多个指标项

- **计算类型**: 选择"表达式"
- **计算表达式**: 填写数学表达式
- **关联指标项**: 系统自动从表达式中解析
- **单位**: 填写计算结果单位

**支持的运算符**:
- 加法: `+`
- 减法: `-`
- 乘法: `*`
- 除法: `/`
- 括号: `()`

**表达式示例**:
```
a0027/a0329
(a0001 + a0002) / a0003 * 100
```

**注意事项**:
1. 表达式中的指标项编码必须已存在
2. 除法运算会自动处理除数为0的情况
3. 系统会自动解析表达式中的指标项,无需手动填写"依赖指标项"字段

#### 步骤4: 配置科室下钻(可选)

如果需要支持科室下钻分析:

- **支持科室下钻**: 选择"是"

**前提条件**:
1. 关联的指标项SQL必须查询包含 `B16` 字段的表
2. 指标项SQL中的聚合函数必须有 `AS result_value` 别名
3. 通常使用 `d_mr` 表(病案主表)

#### 步骤5: 保存
- 点击"确定"按钮保存

### 修改指标

#### 步骤1: 选择指标
- 在左侧指标树中点击要修改的指标
- 右侧显示指标详情

#### 步骤2: 点击编辑
- 点击右侧详情卡片的"编辑"按钮

#### 步骤3: 修改信息
- 修改需要调整的字段
- **注意**: 修改计算表达式后,系统会自动重新解析依赖的指标项

#### 步骤4: 保存
- 点击"确定"按钮保存修改

#### 步骤5: 刷新指标树
- 修改保存后,系统会自动刷新指标树
- 也可以点击指标树右上角的"刷新"按钮手动刷新

---

## 指标计算

### 批量计算指标

#### 步骤1: 进入指标计算页面
- 点击左侧菜单"指标计算"

#### 步骤2: 选择时间维度

系统支持4种时间维度:

| 时间维度 | 选择器类型 | 时间范围计算 | 使用场景 |
|---------|-----------|------------|---------|
| 按年 | 年份选择器 | YYYY-01-01 至 YYYY-12-31 | 年度统计 |
| 按月 | 月份选择器 | YYYY-MM-01 至 YYYY-MM-最后一天 | 月度统计 |
| 按日 | 日期选择器 | YYYY-MM-DD 至 YYYY-MM-DD | 单日统计 |
| 自定义范围 | 起止日期选择器 | 用户自选 | 任意时间段统计 |

**选择示例**:

**按年计算**:
- 选择时间维度: "按年"
- 选择年份: 2023
- 系统自动设置: 2023-01-01 至 2023-12-31

**按月计算**:
- 选择时间维度: "按月"
- 选择月份: 2023-01
- 系统自动设置: 2023-01-01 至 2023-01-31

**自定义范围**:
- 选择时间维度: "自定义范围"
- 选择开始日期: 2023-03-15
- 选择结束日期: 2023-06-20

#### 步骤3: 选择要计算的指标

**方法1: 手动选择**
- 在指标树中勾选需要计算的末级指标
- 可以勾选多个指标

**方法2: 全选**
- 点击"全选"按钮
- 自动选中所有末级指标

**方法3: 清空选择**
- 点击"清空选择"按钮
- 取消所有选中的指标

#### 步骤4: 开始计算
- 点击"开始计算"按钮
- 系统按顺序计算每个指标

#### 步骤5: 查看计算进度

计算过程中会实时显示进度:

**进度信息**:
- ⏳ **计算中**: 黄色,表示正在计算
- ✅ **计算成功**: 绿色,显示计算结果
- ✅ **计算成功(含科室下钻)**: 绿色,同时执行了科室下钻
- ⚠️ **计算成功(科室下钻失败)**: 橙色,普通计算成功但下钻失败
- ❌ **计算失败**: 红色,显示错误信息

**计算结果展示**:
```
剖宫产率
✅ 计算成功(含科室下钻)
结果: 450 例
```

### 时间维度与计算的对应关系

| 时间维度 | 数据库存储的time_dimension | 数据库存储的time_value | 示例 |
|---------|-------------------------|---------------------|------|
| 按年 | YEAR | yyyy | 2023 |
| 按月 | MONTH | yyyy-MM | 2023-01 |
| 按日 | DAY | yyyy-MM-dd | 2023-01-15 |
| 自定义范围 | CUSTOM | yyyy-MM-dd~yyyy-MM-dd | 2023-01-01~2023-06-30 |

**重要说明**:
- 数据库中存储的是计算时选择的**时间维度**和对应的**时间值**
- 查询结果时需要使用相同的时间维度和时间值才能匹配到数据
- `startDate` 和 `endDate` 只在计算时传递给SQL,不存储在结果表中

---

## 结果查看

### 查看指标统计看板

#### 步骤1: 进入指标统计看板
- 点击左侧菜单"指标统计看板"

#### 步骤2: 选择查询条件

**时间维度选择**: 与计算时保持一致

| 计算时选择 | 查询时应选择 | 时间值格式 |
|-----------|------------|----------|
| 按年 | 按年 | 选择同一年份 |
| 按月 | 按月 | 选择同一月份 |
| 按日 | 按日 | 选择同一日期 |
| 自定义范围 | 自定义范围 | 选择同样的起止日期 |

**查询示例**:

如果计算时选择:
- 时间维度: 按年
- 年份: 2023

则查询时应选择:
- 时间维度: 按年
- 年份: 2023

#### 步骤3: 点击查询
- 点击"查询"按钮
- 系统加载符合条件的计算结果

#### 步骤4: 查看结果

**结果展示格式**:

按指标分组显示,每个指标卡片包含:
- 指标名称和编码
- 计算结果值和单位
- 科室下钻按钮(如果支持)

```
┌─────────────────────────────────┐
│ 剖宫产率                    [科室下钻] │
│ (剖宫产率)                          │
│                                      │
│ 450 例                              │
└─────────────────────────────────┘
```

### 科室下钻查看

#### 步骤1: 点击科室下钻按钮
- 在支持下钻的指标卡片上点击"科室下钻"按钮

#### 步骤2: 查看科室分布

弹出对话框显示各科室的数据:

**表格形式**:
| 科室名称 | 数值 | 占比 |
|---------|------|------|
| 妇产科 | 450 | 35.2% |
| 产科 | 380 | 29.7% |
| 外科 | 285 | 22.3% |
| 其他 | 165 | 12.8% |

**柱状图**:
- 直观展示各科室的数据对比
- 按数值降序排列

---

## 科室下钻功能

### 功能说明

科室下钻允许将指标结果按科室维度进行分解,了解各科室的具体情况。

### 普通计算 vs 科室下钻对比

| 特性 | 普通计算 | 科室下钻 |
|------|---------|---------|
| **接口** | `/api/indicator-result/calculate` | `/api/indicator-result/dept-drill-down` |
| **SQL修改** | ❌ 不修改原始SQL | ✅ 自动添加 `GROUP BY B16` |
| **表要求** | ✅ 任何表都可以 | ⚠️ 必须有B16字段(科室字段) |
| **d_mr表** | ✅ 正常计算 | ✅ 正常计算(有B16字段) |
| **其他表** | ✅ 正常计算 | ❌ 无B16字段则报错500 |
| **返回结果** | 1条汇总记录 | N条记录(每个科室一条) |
| **存储表** | `t_indicator_result` | `t_indicator_result_dept` |
| **结果示例** | 总病案数 = 10000 | 内科=1500, 外科=1200, 妇科=800... |
| **查询接口** | `GET /api/indicator-result/list` | `GET /api/indicator-result/dept-drill/{metricCode}` |

### SQL自动改写机制

**原始SQL**:
```sql
SELECT COUNT(*) AS result_value
FROM d_mr
WHERE A14 >= 18
AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
```

**科室下钻时自动改写为**:
```sql
SELECT B16 as dept_code,
       B16 as dept_name,
       COUNT(*) AS result_value
FROM d_mr
WHERE A14 >= 18
AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
GROUP BY B16
```

**关键点**:
1. 添加 `B16 as dept_code, B16 as dept_name` 到SELECT
2. 添加 `GROUP BY B16` 到SQL末尾
3. 要求原SQL的聚合函数必须有 `AS result_value` 别名

### 常见问题

#### Q1: 为什么科室下钻返回空数据?

**可能原因**:
1. ❌ 指标项SQL查询的表没有B16字段
2. ❌ 数据库中B16字段值为空
3. ❌ 指标项SQL的聚合函数缺少 `AS result_value` 别名
4. ❌ 查询的时间维度或时间值与计算时不匹配

**解决方法**:
1. 确认指标项使用的是 `d_mr` 表
2. 检查SQL中聚合函数是否有 `AS result_value` 别名
3. 查询时使用与计算时相同的时间维度和时间值

#### Q2: 科室下钻报错500?

**原因**: 指标项SQL查询的表没有B16字段

**解决方法**:
- 只对使用 `d_mr` 表的指标启用科室下钻功能
- 或者在其他表中添加B16字段映射

#### Q3: 单指标项能否下钻?

**回答**: ✅ **可以!**

科室下钻与是否有计算表达式无关,关键是:
1. 指标项SQL查询包含B16字段的表
2. 指标项SQL的聚合函数有 `AS result_value` 别名
3. 指标配置中"支持科室下钻"设置为"是"

---

## 操作流程示例

### 完整操作流程: 创建并计算"剖宫产率"指标

#### 第一步: 创建指标项

1. **创建指标项 - 剖宫产总数**
   - 指标项名称: `剖宫产总数`
   - 指标项编码: `a0032`
   - SQL:
   ```sql
   SELECT COUNT(*) AS result_value
   FROM d_mr
   WHERE A37 = '剖宫产'
   AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
   ```

2. **创建指标项 - 产妇总数**
   - 指标项名称: `产妇总数`
   - 指标项编码: `a0033`
   - SQL:
   ```sql
   SELECT COUNT(*) AS result_value
   FROM d_mr
   WHERE A37 IN ('剖宫产', '顺产')
   AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
   ```

#### 第二步: 创建指标

1. **创建父节点**
   - 指标名称: `医疗质量指标`
   - 是否末级: 否

2. **创建叶子节点**
   - 指标名称: `剖宫产率`
   - 父节点: `医疗质量指标`
   - 是否末级: 是
   - 计算类型: 表达式
   - 计算表达式: `a0032 / a0033 * 100`
   - 单位: `%`
   - 支持科室下钻: 是

#### 第三步: 执行计算

1. 进入"指标计算"页面
2. 选择时间维度: `按年`
3. 选择年份: `2023`
4. 勾选"剖宫产率"指标
5. 点击"开始计算"
6. 等待计算完成,查看结果

#### 第四步: 查看结果

1. 进入"指标统计看板"页面
2. 选择时间维度: `按年`
3. 选择年份: `2023`
4. 点击"查询"
5. 查看"剖宫产率"的计算结果

#### 第五步: 科室下钻分析

1. 在"剖宫产率"卡片上点击"科室下钻"按钮
2. 查看各科室的剖宫产率分布
3. 分析哪些科室剖宫产率较高

---

## 附录

### 附录A: 常用数据表字段说明

#### d_mr 表 (病案主表)
| 字段 | 说明 | 示例 |
|------|------|------|
| B15 | 入院日期 | 2023/01/15 |
| B16 | 科室 | 妇产科 |
| A14 | 年龄 | 25 |
| A37 | 分娩方式 | 剖宫产 |
| A48, A49 | 患者标识 | - |

### 附录B: 参数占位符说明

SQL中使用的参数占位符:

| 占位符 | 说明 | 数据类型 | 示例值 |
|--------|------|---------|--------|
| #{startDate} | 开始日期 | String | 2023-01-01 |
| #{endDate} | 结束日期 | String | 2023-12-31 |

### 附录C: 计算状态说明

| 状态 | 图标 | 颜色 | 说明 |
|------|------|------|------|
| 等待计算 | - | 灰色 | 未开始 |
| 计算中 | ⏳ | 黄色 | 正在计算 |
| 计算成功 | ✅ | 绿色 | 计算完成 |
| 计算成功(含科室下钻) | ✅ | 绿色 | 普通计算和科室下钻都成功 |
| 计算成功(科室下钻失败) | ⚠️ | 橙色 | 普通计算成功,下钻失败 |
| 计算失败 | ❌ | 红色 | 计算过程出错 |

---

**文档版本**: v1.0
**更新日期**: 2026-01-10
**系统版本**: 高质量医疗指标管理系统 v1.0
