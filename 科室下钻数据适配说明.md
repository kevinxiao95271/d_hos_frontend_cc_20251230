# 🔧 科室下钻数据适配说明

## 修复日期
2025-12-31

---

## 🎯 需求说明

**用户请求**: "试试看适配下科室下钻功能数据返回与显示"

用户提供了实际的后端API返回格式,需要前端适配以正确显示科室下钻数据。

---

## 📊 数据格式对比

### 后端实际返回格式

**API**: `GET /api/indicator-result/dept-drill/{metricCode}`

**返回示例**:
```json
{
  "code": 200,
  "message": "科室下钻计算完成，共计算 22 个科室",
  "data": [
    {
      "id": 1,
      "metricCode": "肺炎（住院、成人）次均费用",
      "timeDimension": "YEAR",
      "timeValue": "2020",
      "startDate": "2020-01-01",
      "endDate": "2020-12-31",
      "deptCode": "呼吸内科",
      "deptName": "呼吸内科",
      "resultValue": 9696.7500,
      "resultJson": "{\"unit\":\"元\",\"expression\":\"a0046/a0041\",\"metric_name\":\"肺炎（住院、成人）次均费用\",\"item_values\":{\"a0041\":162,\"a0046\":1570853.07},\"metric_code\":\"肺炎（住院、成人）次均费用\",\"result_value\":9696.7500}",
      "createTime": "2025-12-31T09:13:42.000+00:00",
      "updateTime": "2025-12-31T09:13:42.000+00:00"
    },
    {
      "id": 2,
      "deptName": "心内科",
      "resultValue": 8234.5600,
      "resultJson": "{\"unit\":\"元\",\"expression\":\"a0046/a0041\",\"item_values\":{\"a0041\":85,\"a0046\":699937.60},\"result_value\":8234.5600}",
      ...
    }
  ],
  "timestamp": 1735639422681
}
```

### 前端显示需要的格式

**表格列定义** [src/views/indicator-statistics/index.vue:168-199](src/views/indicator-statistics/index.vue#L168-L199):

```javascript
{
  deptName: "呼吸内科",        // 科室名称
  value: 9696.75,              // 指标值
  percentage: 62.3,            // 占比(%)
  sourceValues: {              // 来源指标项的值
    "a0041": 162,
    "a0046": 1570853.07
  }
}
```

---

## ✅ 数据适配实现

### 修改位置

**文件**: [src/views/indicator-statistics/index.vue:442-472](src/views/indicator-statistics/index.vue#L442-L472)

### 适配逻辑

```javascript
// 如果后端返回了数据,使用后端数据
if (deptData && Array.isArray(deptData) && deptData.length > 0) {
  // 1. 计算总值,用于计算百分比
  const total = deptData.reduce((sum, item) => sum + (item.resultValue || 0), 0)

  // 2. 转换数据格式以匹配前端显示需求
  deptDrillData.value = deptData.map(item => {
    // 2.1 解析resultJson获取来源指标项的值
    let sourceValues = {}
    try {
      const jsonData = JSON.parse(item.resultJson || '{}')
      sourceValues = jsonData.item_values || {}
    } catch (e) {
      console.error('Failed to parse resultJson:', e)
    }

    // 2.2 计算百分比
    const percentage = total > 0 ? ((item.resultValue / total) * 100).toFixed(1) : 0

    // 2.3 返回转换后的数据
    return {
      deptName: item.deptName,
      value: item.resultValue,
      percentage: parseFloat(percentage),
      sourceValues: sourceValues
    }
  })
  deptDrillError.value = ''
}
```

---

## 🔍 适配步骤详解

### 步骤1: 计算总值

```javascript
const total = deptData.reduce((sum, item) => sum + (item.resultValue || 0), 0)
```

**目的**: 计算所有科室的指标值总和,用于计算每个科室的占比

**示例**:
```
呼吸内科: 9696.75
心内科: 8234.56
儿科: 7523.45
...
总计: 52341.23
```

### 步骤2: 解析resultJson

```javascript
let sourceValues = {}
try {
  const jsonData = JSON.parse(item.resultJson || '{}')
  sourceValues = jsonData.item_values || {}
} catch (e) {
  console.error('Failed to parse resultJson:', e)
}
```

**目的**: 从JSON字符串中提取来源指标项的值

**输入**:
```json
"{\"item_values\":{\"a0041\":162,\"a0046\":1570853.07},\"result_value\":9696.7500}"
```

**输出**:
```javascript
{
  "a0041": 162,
  "a0046": 1570853.07
}
```

### 步骤3: 计算百分比

```javascript
const percentage = total > 0 ? ((item.resultValue / total) * 100).toFixed(1) : 0
```

**目的**: 计算该科室在总值中的占比

**计算公式**:
```
percentage = (科室值 / 总值) × 100

例如:
呼吸内科占比 = (9696.75 / 52341.23) × 100 = 18.5%
```

### 步骤4: 构建显示数据

```javascript
return {
  deptName: item.deptName,           // 直接使用后端的科室名称
  value: item.resultValue,            // 直接使用后端的结果值
  percentage: parseFloat(percentage), // 转换为数字类型
  sourceValues: sourceValues          // 使用解析出的来源指标项值
}
```

---

## 📋 数据流示例

### 输入(后端返回)

```json
{
  "code": 200,
  "data": [
    {
      "deptName": "呼吸内科",
      "resultValue": 9696.7500,
      "resultJson": "{\"item_values\":{\"a0041\":162,\"a0046\":1570853.07}}"
    },
    {
      "deptName": "心内科",
      "resultValue": 8234.5600,
      "resultJson": "{\"item_values\":{\"a0041\":85,\"a0046\":699937.60}}"
    }
  ]
}
```

### 处理过程

```javascript
// 1. 计算总值
total = 9696.75 + 8234.56 = 17931.31

// 2. 转换第一条数据
{
  deptName: "呼吸内科",
  value: 9696.75,
  percentage: (9696.75 / 17931.31 * 100).toFixed(1) = 54.1,
  sourceValues: {
    "a0041": 162,
    "a0046": 1570853.07
  }
}

// 3. 转换第二条数据
{
  deptName: "心内科",
  value: 8234.56,
  percentage: (8234.56 / 17931.31 * 100).toFixed(1) = 45.9,
  sourceValues: {
    "a0041": 85,
    "a0046": 699937.60
  }
}
```

### 输出(前端显示)

```
┌──────────┬───────────┬────────┬──────────┬──────────┐
│ 科室名称 │ 指标值    │ a0041  │ a0046    │ 占比     │
├──────────┼───────────┼────────┼──────────┼──────────┤
│ 呼吸内科 │ 9,696.75  │ 162    │1,570,853 │ 54.1%    │
│ 心内科   │ 8,234.56  │ 85     │ 699,937  │ 45.9%    │
└──────────┴───────────┴────────┴──────────┴──────────┘
```

---

## 🔍 resultJson结构说明

### 完整结构

```json
{
  "unit": "元",
  "expression": "a0046/a0041",
  "metric_name": "肺炎（住院、成人）次均费用",
  "metric_code": "肺炎（住院、成人）次均费用",
  "result_value": 9696.7500,
  "item_values": {
    "a0041": 162,
    "a0046": 1570853.07
  }
}
```

### 字段说明

| 字段 | 说明 | 前端是否使用 |
|------|------|-------------|
| `unit` | 单位 | ❌ (使用指标定义的单位) |
| `expression` | 计算公式 | ❌ (使用指标定义的公式) |
| `metric_name` | 指标名称 | ❌ (已知) |
| `metric_code` | 指标编码 | ❌ (已知) |
| `result_value` | 结果值 | ❌ (使用`resultValue`字段) |
| `item_values` | **来源指标项的值** | ✅ **提取为`sourceValues`** |

**关键**: 我们只需要提取 `item_values` 字段,其他字段前端已经有了。

---

## 🎯 表格列动态生成

### 来源指标项列

**模板代码** [src/views/indicator-statistics/index.vue:179-192](src/views/indicator-statistics/index.vue#L179-L192):

```vue
<el-table-column
  v-for="item in currentMetric.sourceItems"
  :key="item.itemCode"
  :label="item.itemName"
  width="150"
  align="right"
>
  <template #default="{ row }">
    {{ formatValue(row.sourceValues?.[item.itemCode]) }}
    <span v-if="item.unit" style="margin-left: 4px; color: #999;">
      {{ item.unit }}
    </span>
  </template>
</el-table-column>
```

**说明**:
- `currentMetric.sourceItems`: 当前指标依赖的指标项列表
- `row.sourceValues`: 我们适配后的来源指标项值对象
- `row.sourceValues?.[item.itemCode]`: 通过指标项编码获取对应的值

**示例**:
```javascript
// currentMetric.sourceItems
[
  { itemCode: "a0041", itemName: "肺炎病种例数", unit: "人" },
  { itemCode: "a0046", itemName: "肺炎总费用", unit: "元" }
]

// row.sourceValues (适配后的数据)
{
  "a0041": 162,
  "a0046": 1570853.07
}

// 表格显示
a0041列: 162 人
a0046列: 1,570,853.07 元
```

---

## ✅ 错误处理

### JSON解析失败

```javascript
try {
  const jsonData = JSON.parse(item.resultJson || '{}')
  sourceValues = jsonData.item_values || {}
} catch (e) {
  console.error('Failed to parse resultJson:', e)
}
```

**处理策略**:
- 如果`resultJson`为空或null,使用空对象`{}`
- 如果JSON解析失败,捕获错误并打印到控制台
- `sourceValues`默认为空对象`{}`,不会导致页面崩溃

### 总值为0

```javascript
const percentage = total > 0 ? ((item.resultValue / total) * 100).toFixed(1) : 0
```

**处理策略**:
- 如果总值为0,百分比设为0
- 避免除以0的错误

### resultValue为null

```javascript
const total = deptData.reduce((sum, item) => sum + (item.resultValue || 0), 0)
```

**处理策略**:
- 如果`resultValue`为null或undefined,当作0处理
- 确保`reduce`计算不会出错

---

## 🧪 测试验证

### 测试用例1: 正常数据

**输入**:
```json
{
  "data": [
    {
      "deptName": "呼吸内科",
      "resultValue": 9696.75,
      "resultJson": "{\"item_values\":{\"a0041\":162,\"a0046\":1570853.07}}"
    }
  ]
}
```

**预期输出**:
```javascript
[
  {
    deptName: "呼吸内科",
    value: 9696.75,
    percentage: 100.0,
    sourceValues: { "a0041": 162, "a0046": 1570853.07 }
  }
]
```

### 测试用例2: 空resultJson

**输入**:
```json
{
  "data": [
    {
      "deptName": "儿科",
      "resultValue": 5234.56,
      "resultJson": ""
    }
  ]
}
```

**预期输出**:
```javascript
[
  {
    deptName: "儿科",
    value: 5234.56,
    percentage: 100.0,
    sourceValues: {}  // 空对象,不会报错
  }
]
```

### 测试用例3: 格式错误的JSON

**输入**:
```json
{
  "data": [
    {
      "deptName": "骨科",
      "resultValue": 3156.52,
      "resultJson": "{invalid json}"
    }
  ]
}
```

**预期输出**:
```javascript
[
  {
    deptName: "骨科",
    value: 3156.52,
    percentage: 100.0,
    sourceValues: {}  // 解析失败,返回空对象
  }
]
```

**控制台输出**:
```
Failed to parse resultJson: SyntaxError: Unexpected token i in JSON at position 1
```

---

## 📊 完整示例

### 后端返回22个科室的数据

**API调用**:
```bash
curl -X POST 'http://localhost:8080/dgear/api/indicator-result/dept-drill/肺炎（住院、成人）次均费用?timeDimension=YEAR&startDate=2020-01-01&endDate=2020-12-31'
```

**返回结果**:
```json
{
  "code": 200,
  "message": "科室下钻计算完成，共计算 22 个科室",
  "data": [
    { "deptName": "呼吸内科", "resultValue": 9696.75, "resultJson": "..." },
    { "deptName": "心内科", "resultValue": 8234.56, "resultJson": "..." },
    { "deptName": "儿科", "resultValue": 7523.45, "resultJson": "..." },
    // ... 19 more departments
  ]
}
```

### 前端处理后

```javascript
// deptDrillData.value
[
  { deptName: "呼吸内科", value: 9696.75, percentage: 18.5, sourceValues: {...} },
  { deptName: "心内科", value: 8234.56, percentage: 15.7, sourceValues: {...} },
  { deptName: "儿科", value: 7523.45, percentage: 14.4, sourceValues: {...} },
  // ... 19 more departments
]
```

### 表格显示

```
科室下钻 - 肺炎（住院、成人）次均费用
指标: 肺炎（住院、成人）次均费用 | 时间: 2020-01-01 至 2020-12-31

┌────────────┬────────────┬────────┬──────────────┬────────┐
│ 科室名称   │ 指标值     │ a0041  │ a0046        │ 占比   │
├────────────┼────────────┼────────┼──────────────┼────────┤
│ 呼吸内科   │ 9,696.75 元│ 162 人 │1,570,853.07元│ 18.5%  │
│ 心内科     │ 8,234.56 元│ 85 人  │  699,937.60元│ 15.7%  │
│ 儿科       │ 7,523.45 元│ 93 人  │  699,680.85元│ 14.4%  │
│ ...        │ ...        │ ...    │ ...          │ ...    │
└────────────┴────────────┴────────┴──────────────┴────────┘

共 22 条记录
```

---

## ✨ 总结

### 修改的文件

**文件**: [src/views/indicator-statistics/index.vue:442-472](src/views/indicator-statistics/index.vue#L442-L472)

### 核心改进

1. ✅ **解析resultJson** - 从JSON字符串提取`item_values`作为`sourceValues`
2. ✅ **计算百分比** - 基于所有科室的总值计算每个科室的占比
3. ✅ **数据转换** - 将后端格式转换为前端显示格式
4. ✅ **错误处理** - 优雅处理JSON解析失败和空值情况

### 适配效果

**修复前**:
```
❌ 直接使用后端数据,缺少percentage和sourceValues字段
❌ 表格显示异常或报错
```

**修复后**:
```
✅ 正确解析resultJson获取来源指标项值
✅ 自动计算百分比
✅ 表格完整显示所有列
✅ 优雅处理各种异常情况
```

---

**科室下钻数据适配完成,现在可以正确显示22个科室的详细数据!** 🎉
